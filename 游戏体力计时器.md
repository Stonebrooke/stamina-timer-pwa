# 游戏体力计时器 PWA 方案文档

> 版本：v1.2.2 | 日期：2026-07-11 | 目标：纯前端 PWA，覆盖桌面端 + iOS + Android

---

## 1. 项目概述

本方案基于 PWA（Progressive Web App）技术栈，实现一个跨平台的游戏体力恢复计时器。核心特性包括：

- **多计时器管理**：支持同时追踪多个游戏的体力恢复
- **自定义参数**：每单位体力恢复时间、体力上限、当前体力值
- **实时计算**：基于时间戳自动计算当前体力，无需后台运行
- **本地通知**：体力满时或每 N 点间隔自动弹出通知（依赖 app 保持运行）
- **离线可用**：Service Worker 缓存，无网络也能查看计时器
- **安装到桌面**：支持 Chrome/Edge/Safari 安装为独立应用

> **⚠️ 架构说明（v1.2 重大变更）**：v1.1 采用 Web Push + 后端调度方案，引入了 Redis / Cron / VAPID 等大量后端复杂度。经评估，用户使用场景为"app 常驻后台运行"，因此 v1.2 改为**纯前端本地通知方案**——使用 `Notification API` + 轮询检测，砍掉整个后端。代价是 app 完全关闭后无法接收通知。

---

## 2. 技术选型

| 层级 | 技术 | 用途 |
|------|------|------|
| 前端框架 | 原生 HTML + CSS + JS（ES Module） | 零外部依赖，轻量，易维护 |
| 状态存储 | IndexedDB（原生 API 封装） | 本地持久化计时器数据 |
| 通知服务 | Notification API + Service Worker | 本地通知，无需后端 |
| 离线缓存 | Service Worker | 离线缓存 + 通知显示 |
| 部署 | Vercel / GitHub Pages / Netlify | 纯静态托管，零服务器维护 |

---

## 3. 文件结构

```
stamina-timer-pwa/
├── index.html              # 主页面
├── manifest.json           # PWA 安装清单
├── sw.js                   # Service Worker（缓存 + 通知点击）
├── css/
│   └── style.css           # 样式
├── js/
│   ├── app.js              # 主应用逻辑（含通知轮询检测）
│   ├── db.js               # IndexedDB 封装（纯 CRUD，零业务依赖）
│   ├── notify.js           # 本地通知服务（Notification API 封装）
│   └── timer.js            # 体力计算核心（精确 + 显示双模式）
├── icons/
│   ├── icon-192.png
│   ├── icon-512.png
│   └── icon-maskable-512.png   # Android 自适应图标
└── package.json                # 仅用于部署脚本，无运行时依赖
```

> **变更说明**：移除了 `server/` 目录（常驻后端 + Serverless 方案均删除），`push.js` 重命名为 `notify.js`（从 Web Push 订阅管理简化为本地通知封装）。

---

## 4. 核心功能设计

### 4.1 数据模型

```javascript
// 单个计时器的数据结构
const timerSchema = {
  id: 'uuid-string',          // 唯一标识（UUID 字符串）
  name: '原神',                // 游戏名称
  icon: '🎮',                  // 图标 emoji（最多 4 字符）
  maxStamina: 160,             // 体力上限
  currentStamina: 120,         // 当前体力（基准值，对应 startTime 时刻）
  recoveryMinutes: 8,          // 每点体力恢复分钟数
  startTime: 1690123456789,    // 基准时间戳（ms），currentStamina 对应此时刻
  notifyAtFull: true,          // 满体力时通知
  notifyInterval: 40,          // 每恢复 N 点通知（0=不通知）
  lastNotifiedStamina: 120,    // 上次通知时的体力值（用于轮询去重）
  color: '#4a90d9',            // 主题色（正则校验 #RRGGBB）
  createdAt: 1690123456789,
  updatedAt: 1690123456789     // 最后修改时间
};
```

> **v1.2 新增字段**：`lastNotifiedStamina` — 记录上次触发通知时的体力值，用于轮询检测中防止重复弹窗。app 重启后从 IndexedDB 读取，不会重复通知已过去的阈值。

### 4.2 体力计算核心算法

> **v1.2 修正**：原方案 `getCurrentStamina` 使用 `Math.floor` 截断小数，导致 `timeToFull` 倒计时最多偏差近 `recoveryMinutes` 分钟（倒计时"卡住"每 N 分钟才跳一次）。现拆分为 `getExactStamina`（含小数，用于计算）和 `getCurrentStamina`（截断整数，用于显示），倒计时每秒平滑递减。

```javascript
// js/timer.js
export class StaminaCalculator {

  /**
   * 计算精确体力值（含小数）
   * 用于倒计时计算、阈值检测等需要精度的场景
   */
  static getExactStamina(timer) {
    const now = Date.now();
    const elapsedMinutes = (now - timer.startTime) / 60000;
    const recovered = elapsedMinutes / timer.recoveryMinutes;
    return Math.min(timer.currentStamina + recovered, timer.maxStamina);
  }

  /**
   * 计算当前体力值（截断整数）
   * 用于 UI 显示
   */
  static getCurrentStamina(timer) {
    return Math.floor(this.getExactStamina(timer));
  }

  /**
   * 计算距离满体力还有多久（毫秒）
   * 使用精确体力值，确保倒计时每秒平滑递减
   */
  static timeToFull(timer) {
    const exact = this.getExactStamina(timer);
    const needed = timer.maxStamina - exact;
    if (needed <= 0) return 0;
    return needed * timer.recoveryMinutes * 60000;
  }

  /**
   * 计算距离下一次间隔通知还有多久（毫秒）
   * 返回 null 表示不需要间隔通知
   * 使用精确体力值避免阈值检测偏差
   *
   * 注意：当前通知逻辑由 app.js checkNotificationThreshold 接管，
   * 此方法保留作为工具函数，供未来 UI 展示"下次通知还有多久"使用。
   */
  static timeToNextNotify(timer) {
    if (!timer.notifyInterval || timer.notifyInterval <= 0) return null;
    const exact = this.getExactStamina(timer);
    const currentInt = Math.floor(exact);
    if (currentInt >= timer.maxStamina) return null;

    const nextThreshold = Math.floor(exact / timer.notifyInterval) * timer.notifyInterval + timer.notifyInterval;
    if (nextThreshold > timer.maxStamina) return null;

    const needed = nextThreshold - exact;
    return Math.ceil(needed * timer.recoveryMinutes * 60000);
  }

  /**
   * 计算下一次通知的目标体力值
   *
   * 注意：当前通知逻辑由 app.js checkNotificationThreshold 接管，
   * 此方法保留作为工具函数，供未来 UI 展示使用。
   */
  static getNextNotifyStamina(timer) {
    if (!timer.notifyInterval || timer.notifyInterval <= 0) return null;
    const exact = this.getExactStamina(timer);
    if (exact >= timer.maxStamina) return timer.maxStamina;

    const nextThreshold = Math.floor(exact / timer.notifyInterval) * timer.notifyInterval + timer.notifyInterval;
    return Math.min(nextThreshold, timer.maxStamina);
  }

  /**
   * 格式化剩余时间
   */
  static formatDuration(ms) {
    if (ms <= 0) return '已满';
    const totalMinutes = Math.ceil(ms / 60000);
    const hours = Math.floor(totalMinutes / 60);
    const minutes = totalMinutes % 60;
    if (hours > 0) return `${hours}小时${minutes}分`;
    return `${minutes}分钟`;
  }

  /**
   * 格式化精确倒计时 HH:MM:SS
   */
  static formatCountdown(ms) {
    if (ms <= 0) return '00:00:00';
    const totalSeconds = Math.ceil(ms / 1000);
    const h = Math.floor(totalSeconds / 3600);
    const m = Math.floor((totalSeconds % 3600) / 60);
    const s = totalSeconds % 60;
    return `${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`;
  }
}
```

### 4.3 IndexedDB 数据层（纯 CRUD，零业务依赖）

> **v1.2 修正**：
> 1. 移除 `pushSubscriptions` 表（不再需要 Web Push 订阅）
> 2. `updateTimer` 不再包含业务计算逻辑（`StaminaCalculator` 调用已移至 `app.js`），`db.js` 回归纯数据 CRUD
> 3. `addTimer` 不再设置 `startTime`（由 `app.js` 负责组装完整数据后传入）

```javascript
// js/db.js — 原生 IndexedDB 封装，零外部依赖，纯 CRUD

const DB_NAME = 'StaminaTimerDB';
const DB_VERSION = 2;

function openDB() {
  return new Promise((resolve, reject) => {
    const request = indexedDB.open(DB_NAME, DB_VERSION);

    request.onerror = () => reject(request.error);
    request.onsuccess = () => resolve(request.result);

    request.onupgradeneeded = (event) => {
      const db = event.target.result;

      // timers 表：id 为字符串主键
      if (!db.objectStoreNames.contains('timers')) {
        const store = db.createObjectStore('timers', { keyPath: 'id' });
        store.createIndex('createdAt', 'createdAt', { unique: false });
        store.createIndex('name', 'name', { unique: false });
      }

      // v2: 移除 pushSubscriptions 表（不再需要 Web Push）
      if (event.oldVersion < 2 && db.objectStoreNames.contains('pushSubscriptions')) {
        db.deleteObjectStore('pushSubscriptions');
      }
    };
  });
}

export class TimerDB {

  static async getAllTimers() {
    const db = await openDB();
    return new Promise((resolve, reject) => {
      const tx = db.transaction('timers', 'readonly');
      const store = tx.objectStore('timers');
      const request = store.getAll();
      request.onsuccess = () => resolve(request.result);
      request.onerror = () => reject(request.error);
    });
  }

  static async getTimer(id) {
    const db = await openDB();
    return new Promise((resolve, reject) => {
      const tx = db.transaction('timers', 'readonly');
      const request = tx.objectStore('timers').get(id);
      request.onsuccess = () => resolve(request.result);
      request.onerror = () => reject(request.error);
    });
  }

  /**
   * 添加计时器
   * 注意：业务字段（startTime, lastNotifiedStamina 等）由 app.js 组装后传入
   * db.js 只负责分配 id / createdAt / updatedAt 并写入
   */
  static async addTimer(timer) {
    const db = await openDB();
    timer.id = crypto.randomUUID();
    timer.createdAt = Date.now();
    timer.updatedAt = Date.now();
    return new Promise((resolve, reject) => {
      const tx = db.transaction('timers', 'readwrite');
      tx.objectStore('timers').add(timer);
      tx.oncomplete = () => resolve(timer);
      tx.onerror = () => reject(tx.error);
    });
  }

  /**
   * 更新计时器（纯数据合并 + 写入）
   * 业务逻辑（startTime 重置、体力基准计算）由 app.js 处理后传入
   */
  static async updateTimer(id, changes) {
    const db = await openDB();
    return new Promise((resolve, reject) => {
      const tx = db.transaction('timers', 'readwrite');
      const store = tx.objectStore('timers');
      let updated = null;
      store.get(id).onsuccess = (e) => {
        const data = e.target.result;
        if (!data) { reject(new Error('Timer not found')); return; }
        Object.assign(data, changes, { updatedAt: Date.now() });
        updated = data;
        store.put(data);
      };
      tx.oncomplete = () => resolve(updated);
      tx.onerror = () => reject(tx.error);
    });
  }

  static async deleteTimer(id) {
    const db = await openDB();
    return new Promise((resolve, reject) => {
      const tx = db.transaction('timers', 'readwrite');
      tx.objectStore('timers').delete(id);
      tx.oncomplete = () => resolve();
      tx.onerror = () => reject(tx.error);
    });
  }
}
```

### 4.4 通知服务（本地 Notification API 封装）

> **v1.2 重写**：原 `push.js` 基于 Web Push API + VAPID + 后端订阅管理，现已完全替换为本地 `Notification API` 封装。文件重命名为 `notify.js`。
>
> **v1.2.1 修正**：1) 恢复 iOS standalone 检测（`isIOS()` / `isStandalone()`），避免非安装状态下误导性错误提示；2) `showNotification()` 添加 `await`，确保错误被 try-catch 捕获；3) 通知 `tag` 加入 `targetStamina` 避免同类型通知互相替换。

```javascript
// js/notify.js — 本地通知服务，零后端依赖

export class NotificationService {

  /**
   * 检测浏览器是否支持通知
   */
  static isSupported() {
    return 'Notification' in window;
  }

  /**
   * 检测是否已获得通知权限
   */
  static isPermitted() {
    return this.isSupported() && Notification.permission === 'granted';
  }

  /**
   * 检测是否为 iOS 设备
   */
  static isIOS() {
    return /iPad|iPhone|iPod/.test(navigator.userAgent);
  }

  /**
   * 检测是否以 standalone 模式运行（已添加到主屏幕）
   */
  static isStandalone() {
    return window.matchMedia('(display-mode: standalone)').matches
      || window.navigator.standalone === true;
  }

  /**
   * 请求通知权限
   * 包含 iOS standalone 模式前置检测，避免误导性错误提示
   */
  static async requestPermission() {
    if (!this.isSupported()) {
      return { granted: false, reason: '当前浏览器不支持通知功能' };
    }
    // iOS 必须先添加到主屏幕（standalone 模式）才能使用通知
    if (this.isIOS() && !this.isStandalone()) {
      return {
        granted: false,
        reason: 'iOS 设备需先将应用添加到主屏幕（Safari → 分享按钮 → 添加到主屏幕）才能使用通知功能'
      };
    }
    if (Notification.permission === 'granted') {
      return { granted: true };
    }
    if (Notification.permission === 'denied') {
      return { granted: false, reason: '通知权限已被拒绝，请在浏览器设置中重新允许' };
    }
    const result = await Notification.requestPermission();
    return {
      granted: result === 'granted',
      reason: result === 'granted' ? null : '用户拒绝了通知权限'
    };
  }

  /**
   * 发送本地通知
   * 优先使用 Service Worker 通知（后台标签页也能显示）
   */
  static async notify(timer, type, targetStamina) {
    if (!this.isPermitted()) return;

    const title = type === 'full'
      ? `${timer.name} 体力已满！`
      : `${timer.name} 体力已恢复 ${targetStamina} 点`;
    const body = type === 'full' ? '快去清体力吧～' : '注意体力恢复进度';

    const options = {
      body,
      icon: '/icons/icon-192.png',
      badge: '/icons/icon-192.png',
      tag: `stamina-${timer.id}-${type}-${targetStamina}`,
      requireInteraction: true,
      data: { timerId: timer.id }
    };

    try {
      if ('serviceWorker' in navigator) {
        const reg = await navigator.serviceWorker.ready;
        await reg.showNotification(title, options);
      } else {
        new Notification(title, options);
      }
    } catch (err) {
      console.warn('通知发送失败:', err.message);
    }
  }
}
```

### 4.5 Service Worker

> **v1.2 修正**：移除了 `push` 事件处理器（不再使用 Web Push）。保留 `notificationclick` 用于 Service Worker 通知的点击处理。缓存策略不变（导航请求网络优先，静态资源缓存优先）。
>
> **v1.2.1 修正**：`STATIC_ASSETS` 补充 `icon-maskable-512.png`（原遗漏，导致 Android 自适应图标仅在联网时可用）。

```javascript
// sw.js
const CACHE_NAME = 'stamina-timer-v2.2';
const STATIC_ASSETS = [
  '/',
  '/index.html',
  '/css/style.css',
  '/js/app.js',
  '/js/db.js',
  '/js/timer.js',
  '/js/notify.js',
  '/manifest.json',
  '/icons/icon-192.png',
  '/icons/icon-512.png',
  '/icons/icon-maskable-512.png'
];

// 安装时缓存静态资源
self.addEventListener('install', event => {
  event.waitUntil(
    caches.open(CACHE_NAME).then(cache => cache.addAll(STATIC_ASSETS))
  );
  self.skipWaiting();
});

// 激活时清理旧缓存
self.addEventListener('activate', event => {
  event.waitUntil(
    caches.keys().then(keys =>
      Promise.all(keys.filter(k => k !== CACHE_NAME).map(k => caches.delete(k)))
    )
  );
  self.clients.claim();
});

// 拦截请求 — 分策略处理
self.addEventListener('fetch', event => {
  const request = event.request;

  // 导航请求（HTML 页面）：网络优先，降级缓存
  if (request.mode === 'navigate') {
    event.respondWith(
      fetch(request)
        .then(response => {
          const responseClone = response.clone();
          caches.open(CACHE_NAME).then(cache => cache.put(request, responseClone));
          return response;
        })
        .catch(() => caches.match(request))
    );
    return;
  }

  // 静态资源：缓存优先，降级网络
  event.respondWith(
    caches.match(request).then(response =>
      response || fetch(request).then(fetchResponse => {
        const responseClone = fetchResponse.clone();
        caches.open(CACHE_NAME).then(cache => cache.put(request, responseClone));
        return fetchResponse;
      })
    )
  );
});

// 处理通知点击（Service Worker 通知）
self.addEventListener('notificationclick', event => {
  event.notification.close();
  event.waitUntil(
    clients.matchAll({ type: 'window' }).then(clientList => {
      if (clientList.length > 0) {
        clientList[0].focus();
        return;
      }
      clients.openWindow('/');
    })
  );
});
```

### 4.6 主应用逻辑

> **v1.2 修正**：
> 1. **补全表单提交处理器**（原方案 `bindEvents` 完全缺失 form submit 绑定，app 无法创建/编辑计时器）
> 2. **通知改为轮询检测**：在每秒 `updateDisplay` 循环中检测体力阈值跨越，配合 `lastNotifiedStamina` 去重。删除 `scheduleNotifications` / `scheduleIntervalNotifications` / `callScheduleAPI` / `rescheduleNotifications` 等后端调度方法
> 3. **XSS 防护**：新增 `escapeHtml()` / `validateColor()` / `validateIcon()` 工具函数，所有用户输入在拼入 innerHTML 前转义
> 4. **层级职责分离**：`startTime` 重置、体力基准计算等业务逻辑从 `db.js` 上移到 `app.js`
> 5. **render() 改用 addEventListener** 替代内联 onclick，进一步降低 XSS 面
> 6. **`enablePush` 改为 `enableNotifications`**：调用 `Notification.requestPermission()` 而非 Web Push 注册
>
> **v1.2.1 修正**：
> 1. 表单提交改为 `async` + `try-catch`，IndexedDB 失败时 alert 错误信息而非静默丢失
> 2. 增加 JS 层输入校验（名称非空、体力上限≥1、当前体力≥0 且≤上限、恢复间隔≥1）
> 3. 间隔通知目标值改为最近跨越的阈值，避免 app 关闭后通知内容与实际体力不符
> 4. `init()` 中 `lastNotifiedStamina` 初始化时持久化到 IndexedDB
> 5. 删除 `updateTimer` 中未使用的 `actualStamina` 死代码
> 6. 取消按钮改用 `addEventListener`，删除 `window.closeModal` 全局函数
>
> **v1.2.2 修正**：
> 1. **修复间隔通知 Off-by-one Bug**：`checkNotificationThreshold` 中 `Math.floor((current-1)/interval)*interval` 改为 `Math.floor(current/interval)*interval`，避免体力恰好等于阈值时漏检
> 2. `showEditModal` 中 `currentStamina` 预填改用 `Math.round(getExactStamina())`，避免编辑时丢失小数部分体力进度
> 3. `deleteTimer` 补充 `try-catch` 错误处理，与 `addTimer`/`updateTimer` 保持一致
> 4. `init()` 中检测通知权限已授予时直接恢复按钮为"已开启"状态，避免重载后用户困惑
> 5. `startUpdateLoop` 中添加 `visibilitychange` 监听，页面从后台切回前台时立即刷新 UI 和检测通知阈值

```javascript
// js/app.js
import { TimerDB } from './db.js';
import { StaminaCalculator } from './timer.js';
import { NotificationService } from './notify.js';

// ─── XSS 防护工具函数 ───

function escapeHtml(str) {
  const div = document.createElement('div');
  div.textContent = String(str);
  return div.innerHTML;
}

function validateColor(color) {
  return /^#[0-9a-fA-F]{6}$/.test(color) ? color : '#4a90d9';
}

function validateIcon(icon) {
  if (!icon || icon.length === 0 || icon.length > 4) return '🎮';
  return icon;
}

// ─── 应用主类 ───

class StaminaApp {
  constructor() {
    this.timers = [];
    this.updateInterval = null;
    this.init();
  }

  async init() {
    // 注册 Service Worker
    if ('serviceWorker' in navigator) {
      await navigator.serviceWorker.register('/sw.js');
    }
    // 加载数据
    this.timers = await TimerDB.getAllTimers();
    // 初始化 lastNotifiedStamina（兼容旧数据），并持久化到 IndexedDB
    this.timers.forEach(timer => {
      if (timer.lastNotifiedStamina === undefined) {
        timer.lastNotifiedStamina = StaminaCalculator.getCurrentStamina(timer);
        TimerDB.updateTimer(timer.id, { lastNotifiedStamina: timer.lastNotifiedStamina });
      }
    });
    this.render();
    this.startUpdateLoop();
    this.bindEvents();

    // v1.2.2: 恢复通知按钮状态（已授权时直接显示已开启）
    if (NotificationService.isPermitted()) {
      const btn = document.getElementById('enable-notifications');
      btn.textContent = '✅ 通知已开启';
      btn.disabled = true;
    }
  }

  bindEvents() {
    document.getElementById('add-timer').addEventListener('click', () => this.showAddModal());
    document.getElementById('enable-notifications').addEventListener('click', () => this.enableNotifications());
    document.getElementById('cancel-btn').addEventListener('click', () => this.closeModal());

    // v1.2 补全：表单提交处理器
    document.getElementById('timer-form').addEventListener('submit', async (e) => {
      e.preventDefault();
      const form = e.target;
      const formData = {
        name: form.name.value.trim(),
        icon: form.icon.value.trim(),
        maxStamina: parseInt(form.maxStamina.value),
        currentStamina: parseInt(form.currentStamina.value),
        recoveryMinutes: parseInt(form.recoveryMinutes.value),
        notifyAtFull: form.notifyAtFull.checked,
        notifyInterval: parseInt(form.notifyInterval.value) || 0,
        color: form.color.value
      };

      // 输入校验
      if (!formData.name) {
        alert('请输入游戏名称');
        return;
      }
      if (isNaN(formData.maxStamina) || formData.maxStamina < 1) {
        alert('体力上限必须为正整数');
        return;
      }
      if (isNaN(formData.currentStamina) || formData.currentStamina < 0) {
        alert('当前体力不能为负数');
        return;
      }
      if (formData.currentStamina > formData.maxStamina) {
        alert('当前体力不能超过体力上限');
        return;
      }
      if (isNaN(formData.recoveryMinutes) || formData.recoveryMinutes < 1) {
        alert('恢复间隔必须为正整数（分钟）');
        return;
      }

      try {
        const modal = document.getElementById('modal');
        if (modal.dataset.mode === 'edit') {
          await this.updateTimer(modal.dataset.timerId, formData);
        } else {
          await this.addTimer(formData);
        }
        this.closeModal();
      } catch (err) {
        console.error('保存失败:', err);
        alert('保存失败: ' + err.message);
      }
    });
  }

  startUpdateLoop() {
    // 每秒更新显示 + 检测通知阈值
    this.updateInterval = setInterval(() => this.updateDisplay(), 1000);

    // v1.2.2: 页面从后台切回前台时立即刷新（iOS 后台 JS 冻结、桌面后台标签页节流）
    document.addEventListener('visibilitychange', () => {
      if (!document.hidden) {
        this.updateDisplay();
      }
    });
  }

  /**
   * 每秒执行：更新 UI 显示 + 轮询检测通知阈值
   */
  updateDisplay() {
    this.timers.forEach(timer => {
      const current = StaminaCalculator.getCurrentStamina(timer);
      const toFull = StaminaCalculator.timeToFull(timer);
      const countdown = StaminaCalculator.formatCountdown(toFull);

      // 更新 UI
      const el = document.querySelector(`[data-timer-id="${timer.id}"]`);
      if (el) {
        el.querySelector('.current-stamina').textContent = `${current}/${timer.maxStamina}`;
        el.querySelector('.countdown').textContent = countdown;
        el.querySelector('.progress-fill').style.width = `${(current / timer.maxStamina) * 100}%`;
        el.classList.toggle('is-full', current >= timer.maxStamina);
      }

      // 轮询检测通知阈值
      this.checkNotificationThreshold(timer, current);
    });
  }

  /**
   * 检测体力是否跨过通知阈值，跨过则弹通知
   * 使用 lastNotifiedStamina 防止重复弹窗
   */
  checkNotificationThreshold(timer, current) {
    const lastNotified = timer.lastNotifiedStamina ?? current;
    if (current <= lastNotified) return;

    let notifyType = null;
    let targetStamina = current;

    // 检测满体力通知
    if (timer.notifyAtFull && current >= timer.maxStamina && lastNotified < timer.maxStamina) {
      notifyType = 'full';
      targetStamina = timer.maxStamina;
    }
    // 检测间隔通知
    else if (timer.notifyInterval > 0 && current < timer.maxStamina) {
      // 取最近跨越的阈值（而非最旧的），避免 app 关闭后通知内容与实际体力不符
      // 注意：不加 -1，否则当 current 恰好等于阈值时会漏检
      const highestCrossed = Math.floor(current / timer.notifyInterval) * timer.notifyInterval;
      if (highestCrossed > lastNotified && highestCrossed < timer.maxStamina) {
        notifyType = 'interval';
        targetStamina = highestCrossed;
      }
    }

    if (notifyType) {
      // 先更新追踪值（同步），防止下一秒重复触发
      timer.lastNotifiedStamina = current;
      // 持久化到 IndexedDB（异步，不阻塞 UI）
      TimerDB.updateTimer(timer.id, { lastNotifiedStamina: current });
      // 发送通知（仅在已授权时）
      if (NotificationService.isPermitted()) {
        NotificationService.notify(timer, notifyType, targetStamina);
      }
    }
  }

  async addTimer(formData) {
    const now = Date.now();
    const timer = {
      name: formData.name,
      icon: validateIcon(formData.icon),
      maxStamina: parseInt(formData.maxStamina),
      currentStamina: parseInt(formData.currentStamina),
      recoveryMinutes: parseInt(formData.recoveryMinutes),
      startTime: now,                              // 由 app.js 设置，非 db.js
      notifyAtFull: formData.notifyAtFull,
      notifyInterval: parseInt(formData.notifyInterval) || 0,
      lastNotifiedStamina: parseInt(formData.currentStamina), // 初始化为当前体力
      color: validateColor(formData.color)
    };
    const saved = await TimerDB.addTimer(timer);
    this.timers.push(saved);
    this.render();
  }

  async editTimer(id) {
    const timer = this.timers.find(t => t.id === id);
    if (!timer) return;
    this.showEditModal(timer);
  }

  /**
   * 更新计时器
   * 业务逻辑（startTime 重置、体力基准计算）在此处理，db.js 只做写入
   */
  async updateTimer(id, formData) {
    const old = await TimerDB.getTimer(id);
    if (!old) throw new Error('Timer not found');

    // 组装完整变更：以表单填入的 currentStamina 为基准，重置 startTime
    const changes = {
      name: formData.name,
      icon: validateIcon(formData.icon),
      maxStamina: parseInt(formData.maxStamina),
      currentStamina: parseInt(formData.currentStamina),
      recoveryMinutes: parseInt(formData.recoveryMinutes),
      startTime: Date.now(),                       // 重置基准时间
      notifyAtFull: formData.notifyAtFull,
      notifyInterval: parseInt(formData.notifyInterval) || 0,
      lastNotifiedStamina: parseInt(formData.currentStamina), // 重置通知追踪
      color: validateColor(formData.color)
    };

    const updated = await TimerDB.updateTimer(id, changes);

    // 更新本地数据
    const index = this.timers.findIndex(t => t.id === id);
    if (index !== -1) {
      this.timers[index] = updated;
    }
    this.render();
  }

  async deleteTimer(id) {
    if (!confirm('确定删除这个计时器？')) return;
    try {
      await TimerDB.deleteTimer(id);
      this.timers = this.timers.filter(t => t.id !== id);
      this.render();
    } catch (err) {
      console.error('删除失败:', err);
      alert('删除失败: ' + err.message);
    }
  }

  /**
   * 开启通知权限
   */
  async enableNotifications() {
    const result = await NotificationService.requestPermission();
    if (result.granted) {
      alert('通知已开启！');
      const btn = document.getElementById('enable-notifications');
      btn.textContent = '✅ 通知已开启';
      btn.disabled = true;
    } else {
      alert(result.reason || '通知开启失败');
    }
  }

  // ─── 模态框 ───

  showAddModal() {
    const modal = document.getElementById('modal');
    modal.querySelector('h2').textContent = '新建计时器';
    modal.querySelector('form').reset();
    modal.dataset.mode = 'add';
    modal.classList.remove('hidden');
  }

  showEditModal(timer) {
    const modal = document.getElementById('modal');
    const form = modal.querySelector('form');
    modal.querySelector('h2').textContent = '编辑计时器';
    modal.dataset.mode = 'edit';
    modal.dataset.timerId = timer.id;

    form.name.value = timer.name;
    form.icon.value = timer.icon || '';
    form.maxStamina.value = timer.maxStamina;
    form.currentStamina.value = Math.round(StaminaCalculator.getExactStamina(timer));
    form.recoveryMinutes.value = timer.recoveryMinutes;
    form.notifyAtFull.checked = timer.notifyAtFull;
    form.notifyInterval.value = timer.notifyInterval || 0;
    form.color.value = timer.color || '#4a90d9';

    modal.classList.remove('hidden');
  }

  closeModal() {
    document.getElementById('modal').classList.add('hidden');
  }

  // ─── 渲染 ───

  render() {
    const container = document.getElementById('timer-list');
    container.innerHTML = '';

    this.timers.forEach(timer => {
      const card = document.createElement('div');
      card.className = 'timer-card';
      card.dataset.timerId = timer.id;
      card.style.setProperty('--theme-color', validateColor(timer.color));

      // 所有用户输入字段经过 escapeHtml 转义
      card.innerHTML = `
        <div class="timer-header">
          <span class="timer-icon">${escapeHtml(timer.icon)}</span>
          <span class="timer-name">${escapeHtml(timer.name)}</span>
          <button class="btn-edit" data-action="edit">⚙️</button>
          <button class="btn-delete" data-action="delete">🗑️</button>
        </div>
        <div class="timer-body">
          <div class="stamina-display">
            <span class="current-stamina">--/${escapeHtml(String(timer.maxStamina))}</span>
            <span class="countdown">--:--:--</span>
          </div>
          <div class="progress-bar">
            <div class="progress-fill"></div>
          </div>
          <div class="timer-meta">
            <span>每 ${escapeHtml(String(timer.recoveryMinutes))} 分钟恢复 1 点</span>
          </div>
        </div>
      `;

      // 使用 addEventListener 替代内联 onclick
      card.querySelector('[data-action="edit"]').addEventListener('click', () => this.editTimer(timer.id));
      card.querySelector('[data-action="delete"]').addEventListener('click', () => this.deleteTimer(timer.id));

      container.appendChild(card);
    });

    this.updateDisplay();
  }
}

const app = new StaminaApp();
window.app = app;
```

---

## 5. 页面结构

```html
<!-- index.html -->
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#1a1a2e">
  <title>游戏体力计时器</title>
  <link rel="manifest" href="/manifest.json">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="app-container">
    <header class="app-header">
      <h1>⏱️ 体力计时器</h1>
      <button id="add-timer" class="btn-primary">+ 新建计时器</button>
      <button id="enable-notifications" class="btn-secondary">🔔 开启通知</button>
    </header>
    <main id="timer-list" class="timer-list">
      <!-- 计时器卡片动态插入 -->
    </main>
  </div>

  <!-- 添加/编辑计时器模态框 -->
  <div id="modal" class="modal hidden">
    <div class="modal-content">
      <h2>新建计时器</h2>
      <form id="timer-form">
        <label>游戏名称 <input type="text" name="name" required placeholder="如：原神"></label>
        <label>图标 <input type="text" name="icon" placeholder="🎮" maxlength="4"></label>
        <label>体力上限 <input type="number" name="maxStamina" required min="1" value="160"></label>
        <label>当前体力 <input type="number" name="currentStamina" required min="0" value="0"></label>
        <label>恢复间隔（分钟） <input type="number" name="recoveryMinutes" required min="1" value="8"></label>
        <label>满体力通知 <input type="checkbox" name="notifyAtFull" checked></label>
        <label>每 N 点通知 <input type="number" name="notifyInterval" min="0" value="0" placeholder="0=不通知"></label>
        <label>主题色 <input type="color" name="color" value="#4a90d9"></label>
        <div class="form-actions">
          <button type="submit" class="btn-primary">保存</button>
          <button type="button" id="cancel-btn" class="btn-secondary">取消</button>
        </div>
      </form>
    </div>
  </div>

  <script type="module" src="/js/app.js"></script>
</body>
</html>
```

```json
// manifest.json
{
  "id": "/",
  "name": "游戏体力计时器",
  "short_name": "体力计时",
  "description": "追踪多个游戏体力恢复，支持本地通知",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#1a1a2e",
  "theme_color": "#1a1a2e",
  "orientation": "portrait",
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png" },
    {
      "src": "/icons/icon-maskable-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "maskable"
    }
  ]
}
```

---

## 6. 部署方案

### 6.1 部署平台对比

| 平台 | 类型 | 推荐场景 |
|------|------|----------|
| Vercel | 静态托管 | 零配置，自动 HTTPS，适合快速部署 |
| GitHub Pages | 静态托管 | 免费，适合开源项目 |
| Netlify | 静态托管 | 自动部署，表单处理 |
| VPS / ECS | 静态文件服务 | 完全控制，需自行配置 HTTPS |

### 6.2 部署步骤（Vercel 为例）

1. 代码推送到 GitHub 仓库
2. 在 Vercel 导入项目（框架选择 "Other"，输出目录为项目根目录）
3. 部署后获得 HTTPS 域名（PWA 需要 HTTPS）
4. 访问网站 → 浏览器地址栏出现安装图标 → 点击"安装"

### 6.3 安装到各平台

| 平台 | 安装方式 |
|------|----------|
| Windows (Chrome/Edge) | 地址栏右侧 🖥️ 图标 → "安装" → 可固定到任务栏 |
| macOS (Chrome) | 菜单 → 更多工具 → 创建快捷方式 → 勾选"作为窗口打开" |
| Android (Chrome) | 底部弹窗 "添加到主屏幕" → 生成独立应用图标 |
| iOS (Safari) | 分享按钮 → "添加到主屏幕" → 生成独立应用 |

---

## 7. 通知兼容性

| 平台 | 支持状态 | 注意事项 |
|------|----------|----------|
| Android Chrome | ✅ 完全支持 | Service Worker 通知最稳定 |
| iOS Safari 16.4+ | ⚠️ 有限支持 | 需先"添加到主屏幕"（standalone 模式），非安装状态下 `Notification.requestPermission()` 返回 denied |
| Windows Chrome/Edge | ✅ 支持 | 系统级通知，与原生应用无异 |
| macOS Chrome | ✅ 支持 | 需系统授权通知权限 |
| Firefox | ✅ 支持 | 桌面端支持良好 |

> **⚠️ 已知限制 — 通知依赖 app 保持运行**：
>
> 本方案使用纯前端 `Notification API` + `setInterval` 轮询检测，通知的触发依赖 JavaScript 持续运行。以下情况通知将无法触发或延迟：
>
> 1. **app 完全关闭**（浏览器/标签页关闭、PWA 进程终止）：**无法接收任何通知**。这是纯前端方案的根本限制。
> 2. **iOS 后台**：iOS 将 app 切到后台后 **JavaScript 执行被完全挂起**（不是节流，是冻结），`setInterval` 完全停止。iOS 用户将 app 切到后台后**不会收到任何通知**，通知仅在 app 处于前台时可用。
> 3. **桌面端后台标签页**：Chrome/Edge/Firefox 在标签页后台时会将 `setInterval` 节流到约每分钟 1 次，通知可能延迟最多 1 分钟。PWA 安装为独立窗口后不受此限制。
>
> 如果需要"app 完全关闭后也能收通知"，需重新引入 Web Push + 轻量后端（见第 10 节后续优化方向）。

---

## 8. 功能清单

- ✅ 创建多个计时器
- ✅ 自定义体力上限
- ✅ 填入当前体力数量
- ✅ 自定义恢复间隔（分钟）
- ✅ 实时显示当前体力值（整数）
- ✅ 倒计时显示（距离满体力，每秒平滑递减）
- ✅ 进度条可视化
- ✅ 满体力本地通知
- ✅ 自定义间隔通知（每 N 点）
- ✅ 通知去重（lastNotifiedStamina 追踪）
- ✅ 离线可用（Service Worker 缓存）
- ✅ 安装到桌面/手机主屏幕
- ✅ 数据本地持久化（IndexedDB 原生 API）
- ✅ 编辑计时器
- ✅ 删除计时器
- ✅ 自定义主题色和图标
- ✅ XSS 防护（escapeHtml + 输入校验）
- ✅ 层级职责分离（db.js 纯 CRUD，业务逻辑在 app.js）

---

## 9. 修正记录

### v1.2.1 → v1.2.2 修复（审查后修正）

| # | 严重度 | 问题 | 修复内容 |
|---|--------|------|----------|
| B1 | 中等 | `checkNotificationThreshold` 间隔通知阈值检测 Off-by-one：`Math.floor((current-1)/interval)*interval` 在体力恰好等于阈值时漏检，延迟 1 点体力才触发 | 去掉 `-1`，改为 `Math.floor(current/interval)*interval`，`> lastNotified` 已足够防重复 |
| B2 | 轻微 | `showEditModal` 用 `getCurrentStamina`（向下取整）预填，编辑保存后丢失小数部分体力进度（最多 `recoveryMinutes` 分钟） | 改用 `Math.round(getExactStamina())` 四舍五入，最大限度减少误差 |
| B3 | 轻微 | `deleteTimer` 通过 `addEventListener` 调用，不在表单 try-catch 覆盖范围内，IndexedDB 删除失败时 unhandled rejection + UI 不一致 | 补充 `try-catch`，失败时 alert 错误信息 |
| B4 | 轻微 | 刷新页面/重开 PWA 后，即使已授予通知权限，"开启通知"按钮仍显示未开启状态 | `init()` 中检测 `isPermitted()`，已授权时直接设置按钮为已开启并 disabled |
| B5 | 增强 | iOS 后台 JS 冻结、桌面后台标签页节流，切回前台后需等待下一个 `setInterval` tick 才刷新 UI（可能延迟 1 分钟） | 添加 `visibilitychange` 监听，页面恢复可见时立即调用 `updateDisplay()` |

### v1.2 → v1.2.1 修复（审查后修正）

| # | 严重度 | 问题 | 修复内容 |
|---|--------|------|----------|
| R1 | 严重 | `showNotification()` 返回 Promise 但未 `await`，推送失败时错误变成 unhandled rejection | 添加 `await` |
| R2 | 严重 | 间隔通知目标值指向最旧遗漏阈值（app 关闭后通知内容与实际体力不符） | 改为取最近跨越的阈值 `Math.floor((current-1)/interval)*interval` |
| R3 | 严重 | iOS standalone 检测回退（v1.1 有 `isStandalone`/`isIOS`，v1.2 删除），非安装状态下错误提示误导用户 | 恢复 `isIOS()` / `isStandalone()` 检测，`requestPermission` 前置校验 |
| R4 | 严重 | 表单提交 `addTimer`/`updateTimer` 未 `await` 也未 `try-catch`，IndexedDB 失败时用户无反馈 | 改为 `async` + `try-catch`，失败时 alert 错误信息 |
| R5 | 中等 | `updateTimer` 中 `actualStamina` 计算后从未使用（死代码） | 删除 |
| R6 | 中等 | `timeToNextNotify` / `getNextNotifyStamina` 定义但从未调用 | 保留作为工具函数，添加注释标注"当前未使用" |
| R7 | 中等 | 第 7 节未说明桌面后台 `setInterval` 节流（通知延迟最多 1 分钟）和 iOS 后台 JS 完全冻结 | 第 7 节补充详细限制说明 |
| R8 | 中等 | 缺少 JS 层输入校验（`currentStamina > maxStamina`、`recoveryMinutes < 1`） | 表单提交时增加校验，不合法时 alert 并阻止提交 |
| R9 | 中等 | `icon-maskable-512.png` 未加入 SW 预缓存列表 | 添加到 `STATIC_ASSETS` |
| R10 | 轻微 | `init()` 中 `lastNotifiedStamina` 初始化仅更新内存未持久化 | 增加 `TimerDB.updateTimer` 调用持久化 |
| R11 | 轻微 | 取消按钮仍用内联 `onclick="closeModal()"`，与"改用 addEventListener"声明不一致 | 改为 `id="cancel-btn"` + `addEventListener`，删除 `window.closeModal` |
| R12 | 轻微 | 通知 `tag` 未包含 `targetStamina`，同类型通知互相替换 | tag 格式改为 `stamina-${id}-${type}-${targetStamina}` |

### v1.1 → v1.2 重大变更

#### 架构级变更

| # | 变更 | 原因 |
|---|------|------|
| A1 | **砍掉整个后端**（server/ 目录、Redis、Cron、VAPID 全部删除） | 用户场景为 app 常驻后台，本地 Notification API + 轮询检测即可满足通知需求，后端复杂度不划算 |
| A2 | **push.js → notify.js** | 从 Web Push 订阅管理简化为 Notification API 封装，~100 行 → ~60 行 |
| A3 | **通知机制改为轮询检测** | 在每秒 updateDisplay 循环中检测阈值跨越，删除 scheduleNotifications / callScheduleAPI / rescheduleNotifications 等后端调度方法群 |
| A4 | **部署简化为纯静态托管** | 无后端，Vercel / GitHub Pages / Netlify 均可 |

#### 缺陷修复

| # | 严重度 | 问题 | 修复内容 |
|---|--------|------|----------|
| F1 | 致命 | 表单提交处理器完全缺失，app 无法创建/编辑计时器 | `bindEvents` 中新增 `form.addEventListener('submit', ...)`，区分 add/edit 模式 |
| F2 | 致命 | `db.js` 使用 `StaminaCalculator` 但未 import，编辑计时器时 ReferenceError 崩溃 | 业务计算逻辑上移到 `app.js`，`db.js` 回归纯 CRUD，零 import |
| F3 | 严重 | 体力计算 `Math.floor` 导致 `timeToFull` 倒计时最多偏差近 `recoveryMinutes` 分钟 | 拆分 `getExactStamina`（含小数）和 `getCurrentStamina`（截断整数），`timeToFull` 改用精确值 |
| F4 | 严重 | `enablePush` 调用已删除的 `PushService.register()` | 改为 `enableNotifications`，调用 `Notification.requestPermission()` |
| F5 | 严重 | `render()` 内联 onclick + 用户输入直接拼入 innerHTML → XSS | 新增 `escapeHtml()` / `validateColor()` / `validateIcon()`，render 改用 `addEventListener` |
| F6 | 中等 | `timeToNextNotify` / `getNextNotifyStamina` 使用 floor 后的整数，阈值检测有偏差 | 改用 `getExactStamina` |
| F7 | 中等 | `db.js` 的 `addTimer` 设置 `startTime`（业务逻辑越界） | `startTime` 由 `app.js` 组装后传入，`db.js` 只负责 id/createdAt/updatedAt |
| F8 | 中等 | `deleteTimer` 调用后端 `DELETE /api/schedule/:timerId`（后端已删除） | 移除后端调用，仅删除本地数据 |
| F9 | 轻微 | `timeToNextNotify` / `getNextNotifyStamina` 的阈值算法在精确值下需调整 | 改用 `Math.floor(exact / interval) * interval + interval` 计算下一阈值 |

#### 新增字段

| 字段 | 位置 | 用途 |
|------|------|------|
| `lastNotifiedStamina` | timerSchema | 记录上次通知时的体力值，轮询检测中用于去重，持久化到 IndexedDB 防重启重复通知 |

### v1.0 → v1.1 修正记录（历史）

#### 致命缺陷修复

| # | 问题 | 修复内容 |
|---|------|----------|
| 1 | 后端 `setInterval` + 内存 `Map` 与 Serverless 部署冲突 | 提供两套后端方案：常驻后端（Redis + node-cron）和 Serverless（Redis + Vercel Cron） |
| 2 | `editTimer` 方法缺失，点击编辑按钮报错 | 补全 `editTimer` / `updateTimer` 方法 |
| 3 | 主键定义矛盾（`++id` 自增 vs `crypto.randomUUID()` 字符串） | 改用原生 IndexedDB，`keyPath: 'id'` 字符串主键 |

#### 严重缺陷修复

| # | 问题 | 修复内容 |
|---|------|----------|
| 4 | 编辑计时器后未重新调度通知 | 新增 `rescheduleNotifications` |
| 5 | 删除计时器时未清理后端调度 | `deleteTimer` 中增加后端清理调用 |
| 6 | `notifyInterval` 间隔通知未实现 | 新增 `scheduleIntervalNotifications` |
| 7 | `PushManager` 类名与浏览器原生接口冲突 | 重命名为 `PushService` |

#### 中等缺陷修复

| # | 问题 | 修复内容 |
|---|------|----------|
| 8 | CDN 引入 Dexie.js 与"零依赖"声明矛盾 | 改用原生 IndexedDB API 封装 |
| 9 | SW 缓存策略过于简单（全部缓存优先） | 导航请求改为网络优先，静态资源保持缓存优先 |
| 10 | iOS 未检测 standalone 模式 | 新增 `checkPushSupport` / `isStandalone` / `isIOS` 前置检测 |
| 11 | `saveSubscription` 每次清空，多设备冲突 | 改为以 `endpoint` 为 key 的 `put` |
| 12 | `updateTimer` 仅在改 `currentStamina` 时重置 `startTime` | 修改 `recoveryMinutes` / `maxStamina` 时也重置基准 |

> **注**：v1.1 的部分修复（#1-2, #4-6, #8, #11）在 v1.2 中已被架构变更覆盖（后端整体删除），不再适用。保留记录以供追溯。

---

## 10. 后续优化方向

1. **Web Push（可选扩展）**：如果未来需要"app 完全关闭后也能收通知"，可重新引入 Web Push + 轻量后端。建议作为可选模块，不破坏纯前端核心
2. **数据同步**：添加用户账号系统，实现多设备数据同步（需配合后端）
3. **高级通知**：支持自定义通知文案、通知音效、震动
4. **桌面挂件增强**：PWA 无法"始终在最上层"，可通过 Electron 包装器实现（可选扩展）
5. **历史统计**：记录体力使用历史，生成恢复曲线图表
6. **游戏预设模板**：内置常见游戏的体力参数（原神、崩坏星穹铁道、绝区零等），一键创建
