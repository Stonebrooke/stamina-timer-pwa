# 游戏体力计时器 PWA# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb,# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：
# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           ## 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAll# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description":# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vit# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons":# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：**# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)`# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.m# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让 server.mjs 可测试，把路径解析抽成函数并导出。
```
# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让 server.mjs 可测试，把路径解析抽成函数并导出。
```

- [ ] **Step 2:# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让 server.mjs 可测试，把路径解析抽成函数并导出。
```

- [ ] **Step 2: 重构 server.mjs 抽离可测试函数**

Modify `server.mjs`:

```javascript
// server.mjs —# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让 server.mjs 可测试，把路径解析抽成函数并导出。
```

- [ ] **Step 2: 重构 server.mjs 抽离可测试函数**

Modify `server.mjs`:

```javascript
// server.mjs — 零依赖静态文件服务器
import http from 'node:http';
import fs from '# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让 server.mjs 可测试，把路径解析抽成函数并导出。
```

- [ ] **Step 2: 重构 server.mjs 抽离可测试函数**

Modify `server.mjs`:

```javascript
// server.mjs — 零依赖静态文件服务器
import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import { fileURL# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让 server.mjs 可测试，把路径解析抽成函数并导出。
```

- [ ] **Step 2: 重构 server.mjs 抽离可测试函数**

Modify `server.mjs`:

```javascript
// server.mjs — 零依赖静态文件服务器
import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const PORT = parse# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让 server.mjs 可测试，把路径解析抽成函数并导出。
```

- [ ] **Step 2: 重构 server.mjs 抽离可测试函数**

Modify `server.mjs`:

```javascript
// server.mjs — 零依赖静态文件服务器
import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const PORT = parseInt(process.env.PORT || '3000', 10);
const ROOT = path.resolve(__dirname# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让 server.mjs 可测试，把路径解析抽成函数并导出。
```

- [ ] **Step 2: 重构 server.mjs 抽离可测试函数**

Modify `server.mjs`:

```javascript
// server.mjs — 零依赖静态文件服务器
import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const PORT = parseInt(process.env.PORT || '3000', 10);
const ROOT = path.resolve(__dirname);

const MIME = {
  '.html': 'text/html; charset=utf-8# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让 server.mjs 可测试，把路径解析抽成函数并导出。
```

- [ ] **Step 2: 重构 server.mjs 抽离可测试函数**

Modify `server.mjs`:

```javascript
// server.mjs — 零依赖静态文件服务器
import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const PORT = parseInt(process.env.PORT || '3000', 10);
const ROOT = path.resolve(__dirname);

const MIME = {
  '.html': 'text/html; charset=utf-8',
  '.js':   'text/javascript# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让 server.mjs 可测试，把路径解析抽成函数并导出。
```

- [ ] **Step 2: 重构 server.mjs 抽离可测试函数**

Modify `server.mjs`:

```javascript
// server.mjs — 零依赖静态文件服务器
import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const PORT = parseInt(process.env.PORT || '3000', 10);
const ROOT = path.resolve(__dirname);

const MIME = {
  '.html': 'text/html; charset=utf-8',
  '.js':   'text/javascript; charset=utf-8',
  '.m# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让 server.mjs 可测试，把路径解析抽成函数并导出。
```

- [ ] **Step 2: 重构 server.mjs 抽离可测试函数**

Modify `server.mjs`:

```javascript
// server.mjs — 零依赖静态文件服务器
import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const PORT = parseInt(process.env.PORT || '3000', 10);
const ROOT = path.resolve(__dirname);

const MIME = {
  '.html': 'text/html; charset=utf-8',
  '.js':   'text/javascript; charset=utf-8',
  '.mjs':  'text/javascript; charset=utf-# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让 server.mjs 可测试，把路径解析抽成函数并导出。
```

- [ ] **Step 2: 重构 server.mjs 抽离可测试函数**

Modify `server.mjs`:

```javascript
// server.mjs — 零依赖静态文件服务器
import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const PORT = parseInt(process.env.PORT || '3000', 10);
const ROOT = path.resolve(__dirname);

const MIME = {
  '.html': 'text/html; charset=utf-8',
  '.js':   'text/javascript; charset=utf-8',
  '.mjs':  'text/javascript; charset=utf-8',
  '.css':  'text/css; charset=utf-8',
  '.# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让 server.mjs 可测试，把路径解析抽成函数并导出。
```

- [ ] **Step 2: 重构 server.mjs 抽离可测试函数**

Modify `server.mjs`:

```javascript
// server.mjs — 零依赖静态文件服务器
import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const PORT = parseInt(process.env.PORT || '3000', 10);
const ROOT = path.resolve(__dirname);

const MIME = {
  '.html': 'text/html; charset=utf-8',
  '.js':   'text/javascript; charset=utf-8',
  '.mjs':  'text/javascript; charset=utf-8',
  '.css':  'text/css; charset=utf-8',
  '.json': 'application/json; charset=utf-8',
  '.png':  'image/png# 游戏体力计时器 PWA 审查修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审查报告中的 P0/P1/P2 问题，并落地高影响 GUI 改进，使项目健壮性、文档一致性和移动端体验显著提升。

**Architecture:** 保持现有 ES Modules 零运行时依赖架构，仅在边界处（SW 注册、IndexedDB 打开、静态服务器路径解析）增加防御式错误处理。GUI 改进集中在 CSS 与 `app.js` 渲染层，不改动核心算法与数据层接口。

**Tech Stack:** Vanilla JS (ES Modules), IndexedDB, Notification API, Service Worker, Vitest, fake-indexeddb, jsdom

---

## File Structure

本次计划涉及文件：

```
/workspace/
├── package.json                         # 修改：canvas 移为可选/脚本隔离
├── server.mjs                           # 修改：路径遍历防护、decodeURIComponent 容错、流错误处理
├── README.md                            # 修改：修正 server.mjs 行为描述
├── 游戏体力计时器.md                    # 修改：同步 icon/alert/getAllTimers/HTML 示例
├── js/
│   ├── app.js                           # 修改：SW/IDB try-catch、DOM 缓存、GUI 信息增强
│   └── utils.js                         # 修改：Toast transitionend 兜底删除
├── css/
│   └── style.css                        # 修改：移动端 header、空状态、卡片已满态、焦点样式
└── tests/
    ├── app.test.js                      # 修改/新增：error handling、render 相关测试
    └── server.test.js                   # 新增：server.mjs 路径遍历等测试
```

---

## Phase 1: 关键修复 (P0)

### Task 1: 修复 `npm install` 因 canvas 原生编译失败

**背景：** `canvas@2.11.2` 在 Node 24 上原生编译失败，导致 `npm install` 整体报错。`canvas` 仅用于 `scripts/generate-icons.js`，非运行时依赖。

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 将 canvas 改为 `optionalDependencies` 并添加安装后提示**

```json
{
  "name": "stamina-timer-pwa",
  "version": "1.2.2",
  "description": "游戏体力恢复计时器 PWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icons": "node scripts/generate-icons.js",
    "postinstall": "node -e \"console.log('提示：如需生成图标，请确保 canvas 可选依赖安装成功；若失败，可忽略并直接使用 icons/ 目录下已有 PNG。')\"",
    "start": "node server.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "allowScripts": {}
}
```

- [ ] **Step 2: 验证 package.json 语法**

Run: `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).name)"`
Expected: `stamina-timer-pwa`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "build: move canvas to optionalDependencies to avoid npm install failure on Node 24"
```

---

### Task 2: 修复 `server.mjs` 路径遍历与健壮性问题

**背景：** `path.join(ROOT, urlPath)` 后 `startsWith(ROOT)` 可被 `/../workspace-evil/file` 绕过；`decodeURIComponent` 可能抛异常；`createReadStream` 可能因文件删除而崩溃。

**Files:**
- Modify: `server.mjs`
- Create: `tests/server.test.js`

- [ ] **Step 1: 创建 server.mjs 单元测试**

Create `tests/server.test.js`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import http from 'node:http';
import path from 'node:path';

// 动态导入 server 会立即 listen，因此用子进程或重构导出。这里先测试路径解析逻辑。
// 为了让 server.mjs 可测试，把路径解析抽成函数并导出。
```

- [ ] **Step 2: 重构 server.mjs 抽离可测试函数**

Modify `server.mjs`:

```javascript
// server.mjs — 零依赖静态文件服务器
import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const PORT = parseInt(process.env.PORT || '3000', 10);
const ROOT = path.resolve(__dirname);

const MIME = {
  '.html': 'text/html; charset=utf-8',
  '.js':   'text/javascript; charset=utf-8',
  '.mjs':  'text/javascript; charset=utf-8',
  '.css':  'text/css; charset=utf-8',
  '.json': 'application/json; charset=utf-8',
  '.png':  'image/png',
  '.jpg':  'image/jpeg